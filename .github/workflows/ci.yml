name: CI
# Auto-triggers temporarily disabled (shared runner-minutes limit, v0.11 cycle —
# matches the kadr repo). Re-enable by restoring the push/pull_request triggers:
#   push:
#     branches: [main, develop]
#   pull_request:
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - run: swift test
