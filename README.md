# CLA signatures

This orphan branch exists only to hold contributor signatures recorded by the
CLA workflow (`.github/workflows/cla.yml`). It deliberately shares no history
with `main` — it is a ledger, not a copy of the project.

Signatures are written to `signatures/version1/cla.json` by
`contributor-assistant/github-action`. Do not edit that file by hand: it is the
record of who agreed to `CLA.md` and when.
